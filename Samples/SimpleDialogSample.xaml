<Window x:Class="WPFDialogSample.Samples.SimpleDialogSample"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WPFDialogSample.Samples"
        mc:Ignorable="d"
        Title="SimpleDialogSample" Height="140" Width="300" >
    <Grid Margin="10, 10, 10, 10">
        <Grid.RowDefinitions>
            <RowDefinition/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="1" Height="30">Please click any button.</TextBlock>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Center" Name="mainStackPanel">
            <StackPanel.Resources>
                <Style TargetType="Button">
                    <Setter Property="Width" Value="80"/>
                    <Setter Property="Margin" Value="5, 0"/>
                </Style>
            </StackPanel.Resources>

            <Button>Answer1</Button>
            <Button>Answer2</Button>
            <Button>Answer3</Button>
        </StackPanel>
    </Grid>
</Window>
